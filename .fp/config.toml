# FP CLI Configuration

project_id = "proj_01KN5AMTV41PSCZBME1D4822SW"
prefix = "SWITCHYARD"

# Extension config
[extensions.check-before-done]
# Default: "bun run check" — disabled until workspace packages exist
checks = ""
